# app/cache (prod) — Redis 응답 캐시 / placeholder 값만. 실제 비밀 금지.
REDIS_PORT=6379
# prod: 리소스 제한 (배포 사이징에 맞게 조정)
REDIS_MAXMEMORY=2gb
# prod: 캐시 eviction 정책
REDIS_MAXMEMORY_POLICY=allkeys-lru
# prod: 비밀번호 필수 — 배포 전 반드시 교체
REDIS_PASSWORD=CHANGE_ME
