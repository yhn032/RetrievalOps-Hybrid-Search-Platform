# Model Serving — prod (최소 런타임 / 보안 활성 / 자원 한도)
APP_PORT=8001
EMBEDDING_MODEL=CHANGE_ME
RERANKER_MODEL=CHANGE_ME
# 디바이스: cpu | gpu — GPU·vLLM 등 런타임 선택은 M2에서 확정
DEVICE=cpu
LOG_LEVEL=info
# 워커별 모델 로딩 메모리를 고려해 M2에서 조정
WORKERS=2
RELOAD=false
